spring:
  application:
    name: auth-service
  cloud:
    nacos:
      server-addr: ${nacos-addr}:8848
      discovery:
        namespace: ${spring.profiles.active}
        group: teachmall-project
      config:
        namespace: ${spring.profiles.active}
        group: teachmall-project
        file-extension: yaml
        refresh-enabled: true
        shared-configs:
          - data-id: database-${spring.profiles.active}.yaml
            group: commom
            refresh: true
          - data-id: feign-${spring.profiles.active}.yaml
            group: commom
            refresh: true
          - data-id: wx-${spring.profiles.active}.yaml
            group: commom
            refresh: true
          - data-id: redis-${spring.profiles.active}.yaml
            group: commom
            refresh: true



  profiles:
    active: ${spring-profile-used}
#weixin:
#  appid: wxed9954c01bb89b47
#  secret: a7482517235173ddb4083788de60b90e